# Aeiro Canteen

A simple, responsive canteen ordering website built with plain HTML, CSS, and JavaScript.

## Run locally

From the project folder, start a local server:

```bash
python -m http.server 8000
```

Then open http://localhost:8000/ in your browser.
